
dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation Deps.androidx_annotations
    
    if (useMaven) {
        api Deps.jump_annotation
    } else {
        api project(':lib-jump-annotation')
    }
}

