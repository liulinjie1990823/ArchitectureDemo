dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation Deps.androidx_fragment
    if (useMaven) {
        api Deps.component_annotation
    } else {
        api project(':lib-component-annotation')
    }
}

