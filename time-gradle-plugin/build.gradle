apply plugin: 'groovy'

dependencies {
    //gradle sdk
    compile gradleApi()
    //groovy sdk
    compile localGroovy()
}

repositories {
    jcenter()
}

//apply from: "bintrayUpload.gradle"
apply from: "pluginBintrayUpload.gradle"
//apply from: "uploadArchives.gradle"
