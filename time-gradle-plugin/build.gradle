apply plugin: 'groovy'

repositories {
    jcenter()
}

dependencies {
    //gradle sdk
    compile gradleApi()
    //groovy sdk
    compile localGroovy()
}

apply from: "uploadArchives.gradle"
apply from: "pluginBintrayUpload.gradle"
